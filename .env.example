# Fly.io Sprites Configuration
FLY_API_TOKEN=your_fly_api_token_here
SPRITES_ORG_SLUG=personal

# Sprite Checkpoints (set these after creating Sprites on Day 1)
BASE_CHECKPOINT=base-authed
REPO_CHECKPOINT=repo-ready

# Optional: Auth token for API (set later for public deploy)
# AUTH_TOKEN=generate_with_openssl_rand_hex_32
